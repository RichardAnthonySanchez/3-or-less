/*
function componentCategory() {
  const element = document.createElement("div");
  element.id = "container";

  element.innerText = "hello from category ts";

  return element;
}

document.body.appendChild(componentCategory());
*/
