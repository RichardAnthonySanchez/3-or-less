/*
function componentProduct() {
  const element = document.createElement("div");
  element.id = "container";

  element.innerText = "hello from product ts";

  return element;
}

document.body.appendChild(componentProduct());
*/
